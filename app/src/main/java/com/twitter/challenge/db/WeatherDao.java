package com.twitter.challenge.db;

//import android.arch.lifecycle.LiveData;
//import android.arch.persistence.room.Insert;
//import android.arch.persistence.room.OnConflictStrategy;
//import android.arch.persistence.room.Query;
//import android.arch.persistence.room.Update;

import com.twitter.challenge.model.Weather;

import java.util.List;

/**
 * Created on 7/13/19.
 */

public interface WeatherDao {

//    // The conflict strategy defines what happens,
//    // if there is an existing entry.
//    // The default action is ABORT.
//    @Insert(onConflict = OnConflictStrategy.REPLACE)
//    void insert(Weather weather);
//
//    // Update multiple entries with one call.
//    @Update
//    public void updateWeather(Weather... weths);
//
//    // Simple query that does not take parameters and returns nothing.
//    @Query("DELETE FROM weather_table")
//    void deleteAll();
//
//    // Simple query without parameters that returns values.
//    @Query("SELECT * from weather_table ORDER BY word ASC")
//    LiveData<List<Weather>> getAllWeather();
//
//    // Query with parameter that returns a specific Weather.
//    @Query("SELECT * FROM weather_table WHERE word LIKE :weth ")
//    public List<Weather> findWeather(String weth);

}
